//We have an array, have to change its first element and show new 
//array's elements as a string.

//Solution:


let r = ['Hello' , ' world'];
r[0] = 'Hi'; //change the first element
console.log(r[0] + r[1]);